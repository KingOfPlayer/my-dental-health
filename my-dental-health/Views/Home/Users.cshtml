@model IQueryable<Entity.User.User>

@foreach(var user in Model)
{
	@user.Id
	<br />
	@user.Name
	<br />
	@user.Surname
	<br />
	@user.Email
	<br />
	@foreach(var role in user.Roles)
	{
		@role.UserRoleId
	}
	<br />
	@user.Password
}